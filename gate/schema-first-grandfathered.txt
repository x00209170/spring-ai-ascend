# Grandfathered prose enum sites — pre-W2.x existing taxonomies allowed to keep prose form
# under Rule 48 (Schema-First Domain Contracts) / gate Rule 60.
#
# Every entry below names an architecture-corpus site that defines a domain enum in prose
# (e.g. `RunMode = GRAPH | AGENT_LOOP`) WITHOUT a sibling yaml schema under
# docs/contracts/. These sites predate the introduction of Rule 48 (ADR-0077 /
# W2.x Phase 6) and are allowed to keep prose form ONLY until the per-entry
# sunset_date passes. After that, gate Rule 60 fails closed until the entry is
# either retrofitted to a yaml schema + Java type pair OR the sunset_date is
# moved forward via an ADR with explicit rationale.
#
# Format: <file_path>|<sunset_date YYYY-MM-DD>|<short description of grandfathered taxonomy>
#
# Date semantics:
#   2026-09-30 — default W3 retrofit cutover (per CLAUDE-deferred.md 48.b).
#   2026-12-31 — entries whose underlying capability lands in W4 (planner toolset).
#   2027-12-31 — entries whose authoritative form is ALREADY a Java enum + schema-
#     layer constraint (RunStatus DFA, IdempotencyRecord CHECK constraint); the
#     prose mention is a documentation cross-reference, not the canonical source.
#     The far-out sunset reflects that the retrofit is structural cleanup, not a
#     semantic capability gap.
#
# New enum taxonomies added AFTER 2026-05-16 MUST cite a yaml schema under
# docs/contracts/ or docs/governance/, NOT add a new line here. The grandfather
# list is closed to new additions — gate Rule 60 self-tests assert no new entries.
# Advancing an existing entry's sunset_date later (forgiveness) requires an ADR
# referenced inline in the description.
#
# Authority: ADR-0077 (Schema-First Domain Contracts); P-M cross-cutting invariant;
# CLAUDE-deferred.md 48.b (W3 retrofit schedule).

ARCHITECTURE.md|2026-09-30|RunMode discriminator (GRAPH | AGENT_LOOP) at §source-tree line 118 — Java enum lives at agent-service/src/main/java/.../runs/RunMode.java
ARCHITECTURE.md|2027-12-31|RunStatus DFA legal transitions (PENDING → RUNNING | CANCELLED; etc.) at §4 #20 / lines 341-343 — Java enum + RunStateMachine validator already authoritative; prose is cross-reference
ARCHITECTURE.md|2026-09-30|JoinPolicy (ALL | ANY | N_OF) and ChildFailurePolicy (PROPAGATE | IGNORE | COMPENSATE) at §4 #19 / line 336 — SuspendReason taxonomy, retrofit alongside W2 contract docs
ARCHITECTURE.md|2026-09-30|SemanticOntology tag (FACT | PLACEHOLDER | HYPOTHESIS | REDACTED) at §4 #25 / line 385 — CausalPayloadEnvelope contract, retrofit at W2
ARCHITECTURE.md|2026-09-30|SkillKind discriminator (JAVA_NATIVE | MCP_TOOL | SANDBOXED_CODE_INTERPRETER) at §4 #26 / line 401 — CapabilityRegistry contract, retrofit at W2
ARCHITECTURE.md|2026-09-30|SkillTrustTier (VETTED | UNTRUSTED) at §4 #27 / line 410 — Skill SPI contract, retrofit at W2
ARCHITECTURE.md|2026-12-31|RunScope discriminator (STEP_LOCAL | SWARM) at §4 #29 / line 429 — Run hierarchy contract, retrofit at W4 planner toolset
ARCHITECTURE.md|2026-09-30|AdmissionDecision (Accepted | Delayed | Rejected | Yielded) at §4 #52 / line 719 — Long-Connection Containment contract (ADR-0050), retrofit at W2
ARCHITECTURE.md|2026-09-30|BackpressureSignal (LOCAL_SATURATION | SKILL_SATURATION | TENANT_QUOTA_EXCEEDED | SHUTDOWN) at §4 #52 / line 721 — Long-Connection Containment contract (ADR-0050), retrofit at W2
agent-service/ARCHITECTURE.md|2027-12-31|IdempotencyRecord status CHECK constraint (CLAIMED|COMPLETED|FAILED) at line 84 — schema-layer enforcer E13 already authoritative; prose is cross-reference
