# Delivery evidence -- 2026-05-08 -- SHA f98dbae

> Cycle-13 Phase B step 1 evidence at architectural SHA `f98dbae`.
> Per cycle-9 sec-A2 / D2 / E1: `evidence_valid_for_delivery=true`;
> `dependency_mode: not_applicable`; `rule_8_eligible: false`; every
> refresh capability remains maturity **L0** until user runs mvn locally
> and HealthEndpointIT passes.

## SHA-current confirmation

Audit-trail diff vs `f98dbae` is restricted to:

- `docs/delivery/2026-05-08-f98dbae.md` (this file)
- `docs/governance/evidence-manifest.yaml`
- `docs/governance/architecture-status.yaml`
- `docs/governance/current-architecture-index.md`
- `gate/log/f98dbae-posix.json`
- `gate/log/self-test-f98dbae.json`

## What this delivery covers

Cycle-13 is the **first commit producing real Java + Maven artifacts**. Phase B
step 1 per the user-confirmed Option A (autonomous, greenfield, no
stock-conflict interference).

Files added in `f98dbae`:

| Concern | Files |
|---|---|
| Maven scaffolding | `pom.xml` (parent) + `agent-{platform,runtime,eval}/pom.xml` |
| W0 main class | `agent-platform/src/main/java/fin/springai/platform/PlatformApplication.java` |
| W0 controller | `web/HealthController.java` + `web/WebSecurityConfig.java` |
| W0 persistence | `persistence/HealthCheckRepository.java` + `db/migration/V1__init.sql` |
| W0 config | `src/main/resources/application.yml` |
| W0 test | `src/test/java/fin/springai/platform/HealthEndpointIT.java` (Testcontainers) |
| Probe (U1->U2) | `agent-{platform,runtime}/src/main/java/.../probe/OssApiProbe.java` |
| Containers | `Dockerfile` (2-stage Maven + distroless) |
| Compose | `ops/compose.yml` (Postgres 16 + agent-platform) |
| CI | `.github/workflows/ci.yml` (mvn verify + gates) |

Manifest changes:

- `rule_8.state` advanced: `fail_closed_artifact_missing` -> `fail_closed_needs_build`
  (cycle-13 P-B step 1 partial; W4 unblocks fully).
- `rule_8.unblocks_at`: W0 -> W4 (real Rule 8 flow requires real-dependency runs, which is W4).
- Operator-shape smoke gate: tri-state outcome (FAIL_ARTIFACT_MISSING / FAIL_NEEDS_BUILD / FAIL_NEEDS_REAL_FLOW).

## Phase B step 1 status (autonomous Option A)

Done by cycle-13:

- [x] Maven multi-module scaffold with BoM-pinned versions
- [x] W0 minimal Spring Boot skeleton with /v1/health
- [x] HealthEndpointIT Testcontainers test
- [x] Probe code per critical-path dep (cited APIs imported)
- [x] Dockerfile + compose + CI workflow
- [x] Operator-shape smoke gate tri-state

Pending the user (Option A's caveat: author env lacks Java + Maven):

- [ ] `mvn -B dependency:resolve` from local Maven cache (verifies version availability)
- [ ] `mvn -B compile` (verifies probe code; advances U1 -> U2)
- [ ] `mvn -B verify` (runs HealthEndpointIT; if green -> capability `agent_platform_facade` advances L0 -> L1; W0 acceptance gate hits)

Once those three pass, the maturity headline updates and operator-shape
smoke gate advances to FAIL_NEEDS_REAL_FLOW (W4-only blocker).

## Architecture-sync gate run

| Field | Value |
|---|---|
| Script | `gate/check_architecture_sync.sh` |
| Version banner | `cycle-9-truth-cut` |
| `semantic_pass` | `true` |
| `evidence_valid_for_delivery` | `true` |

Self-test: `pass` (mode=local-smoke; pass_type=delivery_valid_pass).

Operator-shape smoke: `FAIL_NEEDS_BUILD` (cycle-13 expected state).

## Honest gaps (from author's environment)

- `mvn`, `java` not present in author env -- cannot run actual Maven
  commands. The committed pom.xml + Java + tests are doc-level
  artifacts; their correctness is verified only by the user's local
  `mvn` runs or by CI's `actions/setup-java@v4` step.
- `mvnw` (Maven wrapper) not generated -- the Apache mirror returned
  404 for the wrapper download. User regenerates via
  `mvn wrapper:wrapper` if desired.
- Spring AI starter artifact name (`spring-ai-starter-vector-store-pgvector`
  in agent-runtime/pom.xml) is the cycle-13 best-guess at the 1.0.7
  artifact id; if it differs, agent-runtime/pom.xml is a 1-line fix.
- `pgvector` Postgres extension is NOT installed by ops/compose.yml
  in cycle-13; W3 adds an init script. For W0 the health_check table
  is sufficient.
- MCP class names deliberately not cited in the runtime probe
  (2.0.0-M2 milestone; W3 adds the real surface).

## Audit-trail commit shape

- HEAD parent: `f98dbae` (architectural commit).
- HEAD diff vs parent: governance + delivery + log only (subset of
  `allowed_audit_trail_paths`).
