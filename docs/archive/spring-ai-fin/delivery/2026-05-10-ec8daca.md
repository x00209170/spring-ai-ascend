# W0 Delivery Evidence — Steps 9-12 Finishing Work

**SHA**: `ec8daca`
**Date**: 2026-05-10
**Author**: Chao Xing
**Gate**: `gate/check_architecture_sync.sh`

---

## Commits included in this delivery

| SHA | Message |
|---|---|
| `ec8daca` | sdk: Steps 9-12 finishing work — BoM starters, SPI freeze, milestone gate, dev docs |
| `9522726` | sdk: add optional sidecar adapter starters + compose overlays for mem0/graphmemory/docling |
| `36bbaf3` | sdk: scaffold capability starter modules with SPI surface |
| `fb68bcd` | test: freeze public API + SPI surface via ArchUnit (Step 10 initial) |
| `750cb2f` | sdk: introduce spring-ai-fin-dependencies BoM module (Step 9) |
| `18d637e` | gate: style + W0 audit trail re-run (delivery evidence for Boot 4 / AI 2.0-M5 wave) |
| `cd13612` | docs: advance OSS BoM to U2; add integration model + reference-project precedent |
| `ca9bbba` | deps: upgrade to latest stable + Boot 4.0.5 / AI 2.0.0-M5 + integration-model deps |
| `6ecf2e0` | build: add Maven Wrapper for reproducible builds |

---

## Gate results

| Gate | Result | Log |
|---|---|---|
| `check_architecture_sync.sh --local-only` | `semantic_pass: true` | `gate/log/ec8daca-posix.json` |
| `test_architecture_sync_gate.sh` | `4/4 pass` | `gate/log/self-test-ec8daca.json` |
| `check_spring_ai_milestone.sh` | PASS (date < 2026-08-01) | stdout |
| ArchUnit `ApiCompatibilityTest` | `4/4 tests pass` | Maven Surefire |

---

## Steps 9-12 completion summary

### Step 9 — SDK BoM module
`spring-ai-fin-dependencies/pom.xml` updated to include all 9 new starter modules in `<dependencyManagement>`:
- 5 capability starters: memory, skills, knowledge, governance, persistence
- 4 optional adapter starters: mem0, graphmemory, docling, langchain4j-profile

### Step 10 — ApiCompatibilityTest (SPI freeze via ArchUnit)
Extended from 2 to 4 rules:
1. `no_springai_fin_class_imports_competitor_alibaba_cloud_ai` — enforces spring-ai-alibaba exclusion at compile time
2. `platform_api_package_has_no_runtime_internals_dependency` — platform cannot depend on runtime internals
3. `spi_packages_have_no_platform_dependency` — SPI cannot import platform classes (allowEmptyShould: SPI in separate modules)
4. `spi_packages_import_only_java_sdk_types` — SPI must use only `java.*` types (allowEmptyShould: SPI in separate modules)

All 4 tests GREEN.

### Step 11 — Capability starter scaffolding
9 SPI interfaces created across 5 capability starter modules (committed at `36bbaf3`):
- `LongTermMemoryRepository`, `GraphMemoryRepository` (memory)
- `ToolProvider` (skills)
- `LayoutParser`, `DocumentSourceConnector` (knowledge)
- `PolicyEvaluator` (governance)
- `RunRepository`, `IdempotencyRepository`, `ArtifactRepository` (persistence)

### Step 12 — Sidecar adapter starters + compose overlays
4 adapter starter modules + 4 compose overlays (committed at `9522726`):
- `spring-ai-fin-mem0-starter` + `ops/compose/sidecar-mem0.yml` (mem0 v2.0.2)
- `spring-ai-fin-graphmemory-starter` + `ops/compose/sidecar-graphmemory.yml` (Graphiti v0.29.0)
- `spring-ai-fin-docling-starter` + `ops/compose/sidecar-docling.yml` (Docling-serve v1.18.0)
- `ops/compose/sidecar-ragflow.yml` (RAGFlow v0.25.2 — evaluation only, no SDK adapter)
- `spring-ai-fin-langchain4j-profile` (alternate RAG profile)

### Supporting deliverables
- `gate/check_spring_ai_milestone.sh` — fails CI after 2026-08-01 if `spring-ai.version` still contains `-M`
- `docs/cross-cutting/dev-environment.md` — W0 toolchain setup guide
- `docs/cross-cutting/sdk-versioning.md` — SemVer policy + stable contract surface + deprecation process

---

## OSS BoM U1→U2 promotion (from prior wave, now complete)

All W0 critical-path dependencies resolved and compiled at `ec8daca`:
Spring Boot 4.0.5, Spring Cloud 2025.1.1 (Oakwood), Spring AI 2.0.0-M5, MCP Java SDK 1.0.0 GA, Temporal Java SDK 1.35.0, langchain4j 1.14.1, Resilience4j 2.4.0, Caffeine 3.2.4, Flyway 11.19.1, Tika 3.3.0, ArchUnit 1.4.2, Testcontainers, WireMock, RestAssured, Logstash Logback encoder, springdoc-openapi 3.0.3.

---

## Honest gaps

- **Windows gate absent**: `gate/check_architecture_sync.ps1` not run on this author's environment; CI matrix (W1) will produce `gate/log/<sha>-windows.json`.
- **Spring AI 2.0.0-M5 milestone**: GA expected mid-2026; `gate/check_spring_ai_milestone.sh` enforces upgrade deadline of 2026-08-01.
- **Graphiti vs Cognee**: both cloned into `third_party/`; `spring-ai-fin-graphmemory-starter` wires Graphiti by default; cycle-15 confirms or swaps.
- **Rule 8**: full real-dependency operator-shape gate deferred to W4 per `docs/plans/engineering-plan-W0-W4.md`.
- **ArchUnit SPI rules**: `spi_packages_have_no_platform_dependency` and `spi_packages_import_only_java_sdk_types` use `allowEmptyShould(true)` because SPI classes live in separate Maven modules not on `agent-platform`'s compile classpath. These rules will be effective once a multi-module integration test module is added (W1).
