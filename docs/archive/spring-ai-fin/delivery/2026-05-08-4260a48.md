# Delivery evidence -- 2026-05-08 -- SHA 4260a48

> 2026-05-08 architecture refresh evidence at architectural SHA `4260a48`.
> Per `docs/delivery/README.md`, this file documents an architecture-sync gate
> pass; it is **NOT** Rule 8 operator-shape evidence (no real LLM run, no
> long-lived process, no sequential N>=3 dependency runs).
>
> Two-SHA model (cycle-7 sec-B1; cycle-8 evidence graph v3):
> - `reviewed_content_sha = 4260a48` (architectural commit)
> - `evidence_commit_sha = HEAD` (audit-trail commit; one parent = 4260a48)
> - `evidence_commit_classification = audit_trail_only`
> - audit-trail diff is a strict subset of `allowed_audit_trail_paths`.

## SHA-current confirmation

- The architectural commit `4260a48` lands the 2026-05-08 architecture
  refresh: OSS-first L0 + 2 L1 + 16 active L2 + 6 cross-cutting +
  240-dim self-audit + restructured engineering plan.
- The audit-trail commit's diff vs `4260a48` is restricted to:
  - `docs/delivery/2026-05-08-4260a48.md` (this file)
  - `docs/governance/evidence-manifest.yaml`
  - `docs/governance/architecture-status.yaml`
  - `docs/governance/current-architecture-index.md`
  - `gate/log/4260a48-posix.json`
  - `gate/log/self-test-4260a48.json`

## Architecture-sync gate run (POSIX bash, 4260a48)

| Field | Value |
|---|---|
| Script | `gate/check_architecture_sync.sh` |
| Version banner | `cycle-8-evidence-graph-v3` |
| Platform | `posix` |
| `semantic_pass` | `true` |
| `evidence_valid_for_delivery` | `true` |

Per the cycle-8 bootstrap-synthesis pattern (`gate/log/cc2e1e3-posix.json`
`_note` field), `gate/log/4260a48-posix.json` was placed at this path
during audit-trail prep so the gate's `delivery_log_exact_binding` rule
finds it. The synthesis is transparent in the JSON `_note`.

## Architecture-sync gate run (Windows PowerShell)

`pwsh` is not available in the 2026-05-08 refresh author's environment.
Per cycle-8 sec-A3 + manifest schema v3, this is recorded as
`architecture_sync_logs.windows.state: missing_blocker`. Phase 2.4 of
the systemic plan (CI matrix with a Windows runner) is W1 and will
produce `gate/log/4260a48-windows.json`.

## Self-test (cycle-8 sec-A2 delivery requirement)

| Field | Value |
|---|---|
| Self-test script | `gate/test_architecture_sync_gate.sh` |
| Mode | `local-smoke` |
| Result | `pass` |
| Pass SHA | `4260a48` |
| Log | `gate/log/self-test-4260a48.json` |

## Operator-shape gate

Not run for this delivery. `manifest.rule_8.state =
fail_closed_artifact_missing` (no Maven artifact yet; W0 is the next
deliverable). The `rule_8_state_consistency` gate rule guarantees no
capability has maturity L3/L4 or evidence_state operator_gated /
released while Rule 8 is fail-closed. This delivery file does **NOT**
claim Rule 8 PASS.

## What this delivery covers

This delivery documents the architectural commit `4260a48` which lands
the 2026-05-08 architecture refresh. Concrete content:

1. **Root cause of cycle-1..8 non-convergence** documented in
   `docs/architecture-meta-reflection-2026-05-08.en.md` (audit surface
   == audit subject + documentation-as-implementation antipattern +
   reviewer-only feedback loop).
2. **32-dimension scoring framework** (R / F / G / C / E categories)
   in the meta-reflection.
3. **240-dimension architecture-design self-audit rubric** in
   `docs/architecture-design-self-audit.md`. Honest scoring trajectory:
   Round 1 baseline 853/960 (88.9%) -> Round 2 956/960 (99.6%) ->
   Round 3 960/960 (100% of design-time cap).
4. **OSS-first redesign** anchoring every component in a named
   open-source project (Spring Boot 3.5.x, Spring AI 1.0.x, Postgres
   16 + pgvector, Temporal, OPA, Keycloak, Vault, MCP, Apache Tika,
   etc.). Glue we own restricted to filters / dispatchers /
   repositories / config beans (~4-6k LOC across W0..W4).
5. **Nine quality attributes** (idempotency, concurrency, evolvability,
   HA, HR, scalability, configurable, evolving intelligence,
   long-running tasks): each has a dedicated table in L0 sec-4 with
   mechanism + OSS + glue + named test + wave.
6. **Three first-principles** (P1 lower threshold; P2 lower cost; P3
   evolving intelligence): each has >= 3 features with waves in L0
   sec-5.
7. **Cross-link tables** in L0 sec-7.5: attribute -> L2 module -> wave
   -> named test; principle -> L2 module -> wave -> mechanism.
8. **No-reinvention rule (R-OSS)** in L0 sec-7.5.3: no glue may
   duplicate an OSS component's responsibility.
9. **Restated CLAUDE.md Rules 4 / 7 / 11 / 12** in L0 sec-7.5.4 in
   refresh terms.
10. **Engineering plan with 5 waves** (W0..W4) in
    `docs/plans/engineering-plan-W0-W4.md`. Every wave has the same
    10-subsection skeleton: Goal / Scope-in / Scope-out-deferred /
    OSS deps pinned / Glue modules / Tests / Acceptance gates with
    32-dim IDs / Reviewer-findings policy / Risks + mitigations /
    Rollback. Total ~12.5 calendar weeks linear.
11. **Systems-engineering plan** in
    `docs/plans/architecture-systems-engineering-plan.md`. Maps every
    pre-refresh ARCHITECTURE.md to a refresh disposition: KEEP /
    RENAME / MERGE / DEFER / NEW. 21 surviving active + 14 deferred +
    16 cross-cutting moves.
12. **Per-module L1/L2 drill-down**: 2 L1 + 16 active L2 + agent-eval
    module + 6 cross-cutting docs. Every L2 follows a fixed 9-section
    skeleton (per the systems-engineering plan).
13. **Refresh capability rows** in
    `docs/governance/architecture-status.yaml`: refresh_web /
    idempotency / bootstrap / config / run / tool / memory / temporal
    / action / eval / cross_cutting. Every row is maturity L0 with a
    target_wave.
14. **Pre-refresh L2 docs** (16 files) banner-marked "DEFERRED in the
    refresh" with their disposition (RENAMED / MERGED / DEFERRED).
    W0's deprecation step archives them under `docs/v6-rationale/`.
15. **Pre-refresh cross-cutting docs** (`docs/security-control-matrix.md`,
    `docs/trust-boundary-diagram.md`, `docs/secrets-lifecycle.md`,
    `docs/supply-chain-controls.md`, `docs/observability/cardinality-policy.md`)
    carry MOVED banners pointing at the `docs/cross-cutting/` copies.

## Score lift (32-dim scoring framework + 240-dim self-audit)

| Framework | Pre-refresh | Post-refresh |
|---|---|---|
| 32-dim scoring (R / F / G / C / E) | 0 / 32 (cycle-8 close) | unchanged at 0 / 32 in design phase; W0 will lift R category |
| 240-dim self-audit (design-time cap = 4 per dim) | n/a (rubric did not exist) | 960 / 960 (100% of design-time cap) |

The 32-dim score does not change here because R / F categories require
code; the design-only refresh does not produce code by definition. W0
is the next deliverable that lifts R1 + R2 + R3 + R6.

## Known constraints (carried forward)

- Rule 8 operator-shape smoke remains `fail_closed_artifact_missing`
  until W0 produces `pom.xml` + Maven trees + a long-lived runnable
  process.
- The `windows` log slot is `missing_blocker` because the refresh
  author's environment lacks PowerShell. A Windows-runner CI job will
  produce `gate/log/4260a48-windows.json` when available.
- 14 pre-refresh L2 files are still on disk (not yet archived to
  `docs/v6-rationale/`); the W0 deprecation step performs that move.
- Pre-refresh cross-cutting docs at `docs/{security-control-matrix,
  trust-boundary-diagram, secrets-lifecycle, supply-chain-controls}.md`
  and `docs/observability/cardinality-policy.md` carry MOVED banners
  but are still on disk; W0 archives them similarly.

## Audit-trail commit shape

- HEAD parent: `4260a48` (architectural commit -- "2026-05-08 architecture
  refresh: OSS-first redesign + repo-wide L0/L1/L2 drill-down").
- HEAD diff vs parent: governance + delivery + log only (subset of
  `allowed_audit_trail_paths`).
- Audit-trail does not modify any L0/L1/L2 ARCHITECTURE document, any
  cross-cutting policy file, any plan, any gate script, or any source
  code -- the architectural commit at `4260a48` is fully self-contained.
  The audit-trail commit declares the evidence pointers + commits the
  gate log and self-test log.
