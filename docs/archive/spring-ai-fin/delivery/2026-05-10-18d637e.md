# W0 Delivery Evidence

Date: 2026-05-10
Architectural SHA: `18d637e` (W0 implementation: Maven Wrapper + Boot 4 / AI 2.0-M5 upgrade + OSS BoM U2 + Tier C manifest)
Reviewed content SHA: `18d637e`
Plan: `D:\.claude\plans\quiet-coalescing-raven.md` (approved in previous session)

---

## Audit-trail diff constraint

This delivery file is part of an audit-trail commit on top of `18d637e`.
The audit-trail diff touches only paths in `current_head_policy.allowed_audit_trail_paths`:

- `docs/delivery/**` -- this file
- `docs/governance/evidence-manifest.yaml` -- SHA pointers + delivery_file updated
- `docs/governance/architecture-status.yaml` -- latest_semantic_pass_sha + delivery SHA updated
- `docs/governance/current-architecture-index.md` -- delivery entry added
- `gate/log/**` -- architecture-sync log + self-test log copied from gate/log/local/

---

## What W0 covers

W0 implements the approved hybrid library-first SDK plan: Java 21, Maven Wrapper, Boot 4 / AI 2.0-M5, all dep upgrades to latest stable, OSS BoM advanced to U2, and Tier C (Python OSS sidecar) manifest created.

| Commit | Step | Summary |
|---|---|---|
| `ca9bbba` | Step 2 | Maven Wrapper 3.9.15 (`mvnw`, `mvnw.cmd`, `.mvn/wrapper/maven-wrapper.properties`); CI updated to `./mvnw` |
| `2fe6923` | Step 3 | Deps upgraded: Boot 3.5.0 → 4.0.5; Spring Cloud 2025.1.1; Spring AI 1.0.7 → 2.0.0-M5; MCP 2.0.0-M2 → 1.0.0 GA; Temporal 1.34.0 → 1.35.0; Tika 3.3.0; Flyway 11.19.1; Resilience4j 2.4.0; Caffeine 3.2.4; Testcontainers 1.21.4; ArchUnit 1.4.2; springdoc 3.0.3; langchain4j 1.14.1 |
| `cd13612` | Step 4 | OssApiProbe refresh: real MCP 1.0.0 GA imports (`McpClient`, `McpSyncClient`, `McpSchema`); Spring AI 2.0 API surface cited |
| `2517299` | Step 6 | OSS BoM advanced to U2 at sha `cd13612`; integration model + per-capability OSS adoption matrix; excluded deps section (spring-ai-alibaba) |
| `6f7b46d` | Step 7 | `third_party/MANIFEST.md` + `third_party/THIRD_PARTY_NOTICES.md`; all 16 Tier C entries (11 infra + 5 Python OSS sidecars) with version tags + SHAs |
| `18d637e` | cleanup | gate/check_architecture_sync.sh bash conditional style fix (cosmetic) |

Step 1 (Java 21 toolchain): installed locally via Eclipse Temurin; verified `java -version` reports 21.x.
Step 5 (resolve + compile): `mvn -B -ntp dependency:resolve`, then `mvn install -DskipTests`, then `mvn verify` — all GREEN locally. Log: `docs/delivery/2026-05-10-ca9bbba-mvn-resolve.log`.

---

## Gate results

### Architecture-sync gate

Log: `gate/log/18d637e-posix.json` (generated by `bash gate/check_architecture_sync.sh --local-only` at HEAD=`18d637e`)

| Check | Result |
|---|---|
| `working_tree_clean` | false (--local-only; dirty tree from audit-trail file updates) |
| `semantic_pass` | true |
| `evidence_valid_for_delivery` | false (local-only; delivery validity asserted by gate pass at audit-trail commit) |
| `audit_trail_shape` | PASS (HEAD=`18d637e` == reviewed_content_sha → direct case) |
| `rule_8_state_machine_coherent` | PASS (`artifact_present_state: jar_present <-> rule_8.state: fail_closed_needs_real_flow`) |
| `delivery_log_exact_binding` | PASS (`gate/log/18d637e-posix.json` sha=`18d637e`=reviewed_content_sha) |
| `ci_no_or_true_mask` | PASS (no `gate/run_* || true` in CI workflows) |
| `rule_8_state_consistency` | PASS (no L3/L4 capabilities; no Rule 8 PASS claims) |

### Self-test

Log: `gate/log/self-test-18d637e.json`
Result: PASSED (4/4 checks)

### Build verification

`./mvnw -B -ntp dependency:resolve && ./mvnw install -DskipTests && ./mvnw verify` — GREEN locally at sha `cd13612`.
Compile gate: `mvn compile test-compile` GREEN across all modules.
Artifact: JARs produced in `agent-platform/target/` and `agent-runtime/target/`.

### Operator-shape smoke

State: `FAIL_NEEDS_REAL_FLOW` (artifact present; no real-dependency N≥3 sequential runs; W4 deliverable).
Log: `gate/log/local/operator-shape-18d637e-posix.json`
Note: NOT Rule 8 PASS. Full Rule 8 flow (real LLM + real Postgres + N≥3 runs) remains W4 deliverable per `docs/plans/engineering-plan-W0-W4.md`.

---

## OSS BoM U2 promotion summary

All 5 critical-path dependency groups advanced from U1 (doc-cited) to U2 (compile-verified) at sha `cd13612`:

| Group | Representative | U2 evidence |
|---|---|---|
| Spring Boot 4 platform | `spring-boot-starter-web` 4.0.5 | `mvn compile` GREEN; OssApiProbe cites `RestController.class` |
| Spring Security 6 | `spring-security-oauth2-resource-server` | `JwtDecoder.class`, `SecurityFilterChain.class` |
| Spring AI 2.0.0-M5 | `spring-ai-openai-spring-boot-starter` | `ChatClient.class`, `VectorStore.class`, `EmbeddingModel.class` |
| MCP Java SDK 1.0.0 GA | `mcp-core:1.0.0` | `McpClient.class`, `McpSyncClient.class`, `McpSchema.class` (JAR-inspected) |
| Temporal Java SDK 1.35.0 | `temporal-sdk` | `Workflow.class`, `WorkflowClient.class` |

Verified-at-sha: `cd13612` (probe compile + mvn verify GREEN at `18d637e`).

---

## Honest gaps

- **Windows gate log absent**: `pwsh` environment not available. CI matrix (W1 deliverable) will produce `gate/log/18d637e-windows.json`.
- **Spring AI 2.0.0-M5 is a milestone**: GA expected mid-2026. `gate/check_spring_ai_milestone.sh` fails CI past 2026-08-01 if version still contains `-M`. (Step 8 addendum; gate file to be created in W0 addendum commit or W1.)
- **Sidecar adapter starters not scaffolded**: Steps 9–12 (BoM module, ApiCompatibilityTest, capability starters, sidecar adapters) are W0 follow-on work.

---

## Prior delivery chain

- `docs/delivery/2026-05-09-c863c2b.md` -- cycle-14 (A1+A2+B1+B2+C1+C2+D1+D2+E1; ASCII fix; two new gate rules)
- `docs/delivery/2026-05-08-f98dbae.md` -- cycle-13 Phase B step 1
- `docs/delivery/2026-05-08-71b77c6.md` -- cycle-12 Phase A close
- `docs/delivery/2026-05-08-2a29eb5.md` -- cycle-11 OSS BoM
- `docs/delivery/2026-05-08-7b1fa8c.md` -- cycle-10 self-driven review
- `docs/delivery/2026-05-08-e9a692d.md` -- cycle-9 truth-cut
