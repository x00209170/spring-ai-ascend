# Delivery Evidence — cycle-15/16 Defect Closure

**SHA**: `97b0827`
**Date**: 2026-05-10
**Author**: Chao Xing
**Branch**: `fix/cycle-15-defect-closure`

---

## Root cause (Rule 1 four-line block)

**Observed failure**: Five core AutoConfigurations started cleanly in research/prod posture with L0 sentinel impls that throw 500 on first call; counter tags were inconsistent (`method`-only vs `spi+method`); SPI records lacked `tenantId` (Rule 11); README and ARCHITECTURE.md still claimed "Implementation has NOT started"; governance ledger had zero rows for 9 new starters.

**Execution path**: All five core AutoConfigurations created sentinel beans unconditionally (`@ConditionalOnMissingBean` only), with no posture check; sentinel counter tag calls used `("method", "X")` while sidecar sentinels used a different schema; `MemoryMetadata`/`GraphEdge`/`EvaluationResult`/`ToolDescriptor` records were persisted without `tenantId`; documentation and ledger files were never updated after cycles 15-16 delivered the W0 scaffold.

**Root cause**: The W0 scaffold was delivered in cycles 15-16 but the posture fail-fast, counter tag schema, SPI contract spine, and documentation refresh were not completed in those cycles, leaving the codebase in a partially-compliant state after commit 64748f1.

**Evidence**: `spring-ai-fin-memory-starter/src/main/java/fin/springai/runtime/memory/MemoryAutoConfiguration.java` (posture check missing before fix); `docs/governance/architecture-status.yaml` (zero rows for 9 starters before fix); `README.md:97` ("Implementation has NOT started" before fix).

---

## Changes delivered

| Commit | Defects | Description |
|---|---|---|
| `ce1b46a` | E2 | Add `tenantId` to SPI records (Rule 11 spine) |
| `97b0827` | E1+E3+E4 | Posture fail-fast + counter tag schema + slf4j dep |
| `0299011` | D1+D2+D3 | README + ARCHITECTURE.md + L1 docs refresh |
| `335e683` | G1+G2+G3 | Governance ledger: 11 new capability rows + evidence manifest |

---

## Verification

| Check | Result |
|---|---|
| `./mvnw -q -DskipTests=false verify` | BUILD SUCCESS (41/41 tests) |
| `ApiCompatibilityTest` (ArchUnit 4 rules) | GREEN |
| Posture fail-fast tests (5 × 2 = 10 tests) | GREEN |
| Counter tag schema (spi + method) | GREEN |
| `grep README.md -e "pre-implementation" -e "Implementation has NOT started"` | 0 matches |
| `grep "spring-ai-fin-.*-starter" docs/governance/architecture-status.yaml` | ≥9 matches |
| `check_spring_ai_milestone.sh` | PASS (2026-05-10 < 2026-08-01) |

---

## Honest gaps (not in scope for this PR)

- Rule 8 real-dependency N≥3 runs: W4 deliverable.
- No real LLM call end-to-end (R7 still 0): W2 deliverable.
- Test LOC ~670 (R3 target ≥1000): W1 adds default impl tests.
- G1 governance/product LOC ratio still ~4×: W1 product growth addresses.
- 5 sidecar starters skip posture fail-fast (enabled=false protects them).
