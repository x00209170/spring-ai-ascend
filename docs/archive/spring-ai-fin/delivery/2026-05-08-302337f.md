# Delivery 2026-05-08 302337f

> **Classification: ARCHITECTURE-SYNC EVIDENCE.**
> NOT Rule 8 operator-shape evidence. The Rule 8 operator-shape gate (`gate/run_operator_shape_smoke.{ps1,sh}`) exists in `cycle-4-fail-closed` form and produces only `FAIL_ARTIFACT_MISSING` until W0 lands a runnable artifact. Architecture-sync PASS does NOT authorize ship.
>
> Per cycle-3 SHA-current rule: this delivery file is evidence for SHA `302337f` only.

## Cycle context

- **Cycle**: Architecture remediation cycle 5, addressing `docs/systematic-architecture-remediation-plan-2026-05-08-cycle-5.en.md` (11 findings: A1, A2, A3, B1, B2, B3, C1, C2, D1, E1 acknowledged, F1 partial).
- **Predecessor delivery files**: `2026-05-08-d284232.md` (local-only), `2026-05-08-7025ac9.md` (cycle-1+2), `2026-05-08-003ed6f.md` (cycle-3), `2026-05-08-a070a77.md` (cycle-4).
- **Allowed claims**: `proposed`, `design_accepted`, `implemented_unverified`. No capability rises above `implemented_unverified`.

## Posture / shape

- `APP_POSTURE`: not applicable (no runnable artifact).
- `APP_DEPLOYMENT_SHAPE`: not applicable.

## Long-lived process / Real dependencies / Sequential runs / Cross-context resource stability / Lifecycle observability / Cancellation round-trip

Not applicable (no runnable artifact at this SHA).

## What DID run for this SHA — Architecture-sync gate (cycle-5-expanded, POSIX)

| Field | Value (from `gate/log/302337f-posix.json`) |
|---|---|
| Script | `gate/check_architecture_sync.sh` |
| Version | `cycle-5-expanded` |
| Platform | `posix` |
| SHA | `302337f` |
| Outcome | **PASS** |
| `working_tree_clean` | `true` |
| `git_status_porcelain` | `""` (empty) |
| `semantic_pass` | `true` |
| `evidence_valid_for_delivery` | **`true`** |
| `local_only` | `false` |
| `failures` | `[]` |

The Windows PowerShell run will write its evidence to `gate/log/302337f-windows.json` when invoked. Cycle-5 fixed the prior overwrite-each-other behaviour by using platform-suffix filenames (REM-2026-05-08-C5-3).

Cycle-5 gate enhancements verified at this SHA:

1. **Platform-suffix logs (A3)**: `gate/log/<sha>-posix.json` for bash; `gate/log/<sha>-windows.json` for PowerShell. Cross-platform evidence durable for the same SHA.
2. **`rls_reset_vocabulary` scope expansion (C2)**: rule now scans all L0/L1/L2 `ARCHITECTURE.md` files (was only governance/diagram/matrix). Catches reintroduction of `HikariConnectionResetPolicy` in `agent-runtime/server/ARCHITECTURE.md` line 20.
3. **New `hs256_prod_conflict` rule (D1)**: fails when a row in `docs/security-control-matrix.md` mentions `HS256` and `prod` without `rejected` / `not permitted`. Matches the auth L2's "no HS256 path under prod" position.
4. **Server L2 cleanup (C1)**: line 20 no longer lists `HikariConnectionResetPolicy`; replaced with `TransactionStartGucValidator` named without implying reset.
5. **READMEs corrected (B1)**: `gate/README.md` and `docs/delivery/README.md` no longer say `run_operator_shape_smoke.*` doesn't exist. Both reflect the `cycle-4-fail-closed` state truthfully.

## What DID run — Operator-shape smoke gate (fail-closed)

| Field | Value |
|---|---|
| Script | `gate/run_operator_shape_smoke.{ps1,sh}` |
| Version | `cycle-4-fail-closed` |
| Outcome | **FAIL_ARTIFACT_MISSING** (exit 1) |
| `artifact_present` | `false` |
| `evidence_valid_for_delivery` | `false` |
| `rule_8_evidence` | `false` |
| Logs | `gate/log/local/operator-shape-302337f-posix.json` (gitignored); `-windows.json` when PowerShell runs |

This is honest absence-evidence: the architecture's own Rule 8 says no PASS exists at this SHA. Cycle-5 reviewer's E1 finding acknowledged.

## Capability statuses on this SHA

Per `docs/governance/architecture-status.yaml`:

- All P0 + P1 + REM-C1..C4-9 + REM-C5-1..C5-9 findings remain `design_accepted` (none advance past it from this cycle alone).
- `operator_shape_gate` capability:
  - architecture-sync portion: `implemented_unverified`; `last_validated_sha: a070a77`.
  - operator-shape smoke portion: `design_accepted`; `last_validated_sha: null` (fail-closed scripts are absence-evidence, not Rule 8 PASS).
- `operator_gate: null` for `operator_shape_gate` capability.

## Evidence manifest (skeleton; F1 partial)

`docs/governance/evidence-manifest.yaml` introduces a machine-readable manifest binding reviewed SHA, delivery file, per-platform logs, status findings, authoritative index, and Rule 8 state. The full manifest-driven gate rules (`delivery_log_parity`, `manifest_freshness`, `ledger_coverage`, `readme_to_files`, `rule_8_state_consistency`) are W1 deliverables per `REM-2026-05-08-C5-9`. The skeleton lets reviewers confirm the model now exists; W1 makes it enforceable.

## Cycle-5 Definition-of-Done check

| # | Item | Status |
|---|---|---|
| 1 | `git status --short` empty before delivery evidence generation | **Done** at `302337f` (clean tree before this commit) |
| 2 | Final reviewed SHA has committed architecture-sync log + delivery file | **Done** (`gate/log/302337f-posix.json` + this file; the audit-trail commit will add this delivery + log) |
| 3 | Delivery file fields match referenced JSON logs exactly | **Done** for this file (values transcribed from JSON; W1 will gate this automatically) |
| 4 | Windows + POSIX gate results both preserved | **Achieved structurally**: bash run produces `-posix.json`; future PowerShell run will produce `-windows.json` without overwriting |
| 5 | READMEs + actual gate files + status YAML agree on operator-shape state | **Done** (B1 + B2) |
| 6 | server L2 contains no active `HikariConnectionResetPolicy` | **Done** (C1) |
| 7 | RLS vocabulary gate scans server L2 | **Done** (C2 expanded) |
| 8 | Security matrix says HS256 rejected in prod | **Done** (D1) |
| 9 | Cycle-4 findings appear in `architecture-status.yaml` | **Done** (REM-C4-1..C4-9 + REM-C5-1..C5-9) |
| 10 | `current-architecture-index.md` points to latest evidence; banner claims accurate | **Done** (B3) |
| 11 | Operator-shape smoke fails closed pre-W0 | **Done** (cycle-4-fail-closed; W0 replaces with real-flow) |

## Gate result

- **Architecture-sync gate**: **PASS** at SHA `302337f`. `evidence_valid_for_delivery: true`. Log `gate/log/302337f-posix.json`.
- **Operator-shape smoke gate**: **FAIL_ARTIFACT_MISSING** (correct fail-closed behaviour pre-W0). Not Rule 8 evidence.
- **Authorization implied**: this delivery file authorizes nothing beyond confirming the document corpus is internally consistent at SHA `302337f`. Ship requires Rule 8 PASS, which only W0+ implementation produces.

## Recorded by

- Author: Chao Xing (chaos.xing.xc@gmail.com)
- Cycle: 2026-05-08 remediation cycle 5 (commit `302337f`)
- Architecture-sync log: `gate/log/302337f-posix.json`
- Operator-shape smoke logs: `gate/log/local/operator-shape-302337f-{posix,windows}.json` (gitignored; fail-closed pre-W0)
- Evidence manifest: `docs/governance/evidence-manifest.yaml` (skeleton)
- Predecessor delivery: `docs/delivery/2026-05-08-a070a77.md` (cycle-4)
- Next gate: W0 produces the runnable artifact; the operator-shape smoke gate switches to real-flow PASS at that point.
