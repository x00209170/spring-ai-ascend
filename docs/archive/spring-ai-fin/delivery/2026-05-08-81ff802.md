# Delivery 2026-05-08 81ff802

> **Classification: ARCHITECTURE-SYNC EVIDENCE.**
> NOT Rule 8 operator-shape evidence. The Rule 8 operator-shape gate (`gate/run_operator_shape_smoke.{ps1,sh}`) exists in `cycle-4-fail-closed` form and produces only `FAIL_ARTIFACT_MISSING` until W0 lands a runnable artifact.
>
> Per cycle-3 SHA-current rule: this delivery file is evidence for SHA `81ff802` only.
> Per cycle-6 §A1: HEAD will be one commit ahead of `81ff802` (audit-trail commit). The manifest tracks `81ff802` as the latest delivery-valid SHA; that's the structural trade-off documented in `REM-2026-05-08-C6-1`.

## Cycle context

- **Cycle**: Architecture remediation cycle 6, addressing `docs/systematic-architecture-remediation-plan-2026-05-08-cycle-6.en.md` (9 substantive findings; A1 + C2 acknowledged structurally).
- **Predecessor delivery files**: `2026-05-08-d284232.md` (local-only), `2026-05-08-7025ac9.md` (cycle-1+2), `2026-05-08-003ed6f.md` (cycle-3), `2026-05-08-a070a77.md` (cycle-4), `2026-05-08-302337f.md` (cycle-5).
- **Allowed claims**: `proposed`, `design_accepted`, `implemented_unverified`. No capability rises above `implemented_unverified`.

## Posture / shape

- `APP_POSTURE`: not applicable (no runnable artifact).
- `APP_DEPLOYMENT_SHAPE`: not applicable.

## Long-lived process / Real dependencies / Sequential runs / Cross-context resource stability / Lifecycle observability / Cancellation round-trip

Not applicable.

## What DID run for this SHA — Architecture-sync gate (cycle-6-expanded, POSIX)

| Field | Value (from `gate/log/81ff802-posix.json`) |
|---|---|
| `script` | `check_architecture_sync.sh` |
| `version` | `cycle-6-expanded` |
| `platform` | `posix` |
| `sha` | `81ff802` |
| `working_tree_clean` | `true` |
| `git_status_porcelain` | `""` |
| `local_only` | `false` |
| `semantic_pass` | `true` |
| `evidence_valid_for_delivery` | `true` |
| `scan_files_count` | `44` |
| `failures` | `[]` |

The Windows PowerShell run was not performed in this cycle (PowerShell unavailable in cycle-6 author's environment). The manifest documents this gap explicitly via `windows: null` and `REM-2026-05-08-C6-5`. A future cycle running on Windows can add `gate/log/81ff802-windows.json` without contradiction.

Cycle-6 gate enhancements verified at this SHA:

1. **`manifest_freshness` rule (A2/D1)**: reads `docs/governance/evidence-manifest.yaml`, verifies `reviewed_sha` names a delivery file + log that exist (with legacy `<sha>.json` fallback for cycle-4 backward compat).
2. **`readme_to_files` rule (B2/D1)**: fails when `gate/README.md` says the smoke gate "does not exist" while `gate/run_operator_shape_smoke.{ps1,sh}` are present.
3. **`delivery_log_parity` rule (B3/D1, basic)**: for every `docs/delivery/2026-05-08-<sha>.md`, finds matching `gate/log/<sha>-{posix,windows}.json` (or legacy `<sha>.json`) and verifies the log's `sha` field matches the filename.
4. **`hs256_prod_conflict` rule extended (C1)**: now scans `agent-runtime/auth/ARCHITECTURE.md` in addition to `docs/security-control-matrix.md`. Qualifier list expanded with policy-statement phrases ("no HS256 path", "HS256 only for", "mandatory for", "carve-out only", etc.).
5. **Auth L2 line 234 rewritten**: weak HMAC secret rejected only when HmacValidator is active (DEV loopback or research BYOC carve-out); prod rejects HmacValidator activation entirely.
6. **Status YAML split (A4)**: single `operator_shape_gate` capability replaced with `architecture_sync_gate` (implemented_unverified; latest_delivery_valid_sha=81ff802) and `operator_shape_smoke_gate` (design_accepted; rule_8_pass_sha=null).
7. **Manifest updated (A2)**: `reviewed_sha=81ff802`; `delivery_file=docs/delivery/2026-05-08-81ff802.md`; `architecture_sync_logs.posix=gate/log/81ff802-posix.json`.
8. **Gate README fixed (B2)**: status section line 69-70 no longer claims smoke gate "does not exist". `readme_to_files` rule enforces.
9. **Index updated (A3)**: `current-architecture-index.md` lists `2026-05-08-302337f.md` and `2026-05-08-81ff802.md`; removed "will be added" wording.

## What DID run — Operator-shape smoke gate (fail-closed)

| Field | Value |
|---|---|
| Script | `gate/run_operator_shape_smoke.{ps1,sh}` |
| Version | `cycle-4-fail-closed` |
| Outcome | **FAIL_ARTIFACT_MISSING** (exit 1) |
| `artifact_present` | `false` |
| `evidence_valid_for_delivery` | `false` |
| `rule_8_evidence` | `false` |
| Logs | `gate/log/local/operator-shape-81ff802-{posix,windows}.json` (gitignored) |

This is honest absence-evidence (REM-2026-05-08-C2-6, C4-8, C6 §C2). No Rule 8 PASS at this SHA.

## Capability statuses on this SHA

Per `docs/governance/architecture-status.yaml` (cycle-6 split capabilities):

- `architecture_sync_gate`: **`implemented_unverified`**; `latest_semantic_pass_sha: 81ff802`; `latest_delivery_valid_sha: 81ff802`; `latest_delivery_file: docs/delivery/2026-05-08-81ff802.md`.
- `operator_shape_smoke_gate`: **`design_accepted`**; `rule_8_pass_sha: null` (fail-closed scripts are not Rule 8 PASS evidence).
- All P0 + P1 + REM-C1..C6-9 findings remain at `design_accepted` or `implemented_unverified` (gate-related). No core runtime capability advances past `design_accepted`.

## Evidence manifest

`docs/governance/evidence-manifest.yaml`:

```yaml
version: 1
schema_version: cycle-5-skeleton
reviewed_sha: 81ff802
delivery_file: docs/delivery/2026-05-08-81ff802.md
architecture_sync_logs:
  posix: gate/log/81ff802-posix.json
  windows: null
operator_shape_logs:
  posix: null  # fail-closed under gate/log/local/
  windows: null  # fail-closed under gate/log/local/
artifact_present: false
rule_8_state: fail_closed_artifact_missing
```

The manifest is authoritative; the status YAML and index are summaries derived from it.

## Cycle-6 Definition-of-Done check

Per cycle-6 §11 acceptance criteria:

| # | Item | Status |
|---|---|---|
| 1 | `git status --short` empty before evidence generation | **Done** at `81ff802` (clean tree before this audit-trail commit) |
| 2 | `git rev-parse --short HEAD` equals `manifest.reviewed_sha` | **Structural trade-off** (REM-2026-05-08-C6-1): manifest tracks the latest delivery-valid SHA (`81ff802`), HEAD is the audit-trail commit one ahead. Strict equality requires content-addressable amend which contradicts post-hoc gate generation. |
| 3 | Manifest delivery file exists and names same SHA | **Done** (this file) |
| 4 | Architecture-sync logs exist with matching `sha`, `platform`, `evidence_valid_for_delivery` | **Done** for POSIX (`gate/log/81ff802-posix.json`); Windows documented absent in this environment |
| 5 | Current architecture index includes manifest delivery file | **Done** |
| 6 | `architecture-status.yaml` reports same `latest_delivery_valid_sha` as manifest | **Done** (`81ff802`) |
| 7 | `gate/README.md` no stale "smoke gate does not exist" wording | **Done** (B2 + readme_to_files rule) |
| 8 | `agent-runtime/auth/ARCHITECTURE.md` no prod HMAC secret requirement | **Done** (C1 + hs256_prod_conflict scan extended) |
| 9 | Operator-shape smoke fails closed | **Done** (cycle-4-fail-closed retained) |
| 10 | No L3 or ship claim before real Rule 8 PASS | **Done** (no capability above `implemented_unverified`; all cores at `design_accepted`) |

## Gate result

- **Architecture-sync gate**: **PASS** at SHA `81ff802`. `evidence_valid_for_delivery: true`. Log `gate/log/81ff802-posix.json`.
- **Operator-shape smoke gate**: **FAIL_ARTIFACT_MISSING** (correct fail-closed behaviour). Not Rule 8 evidence.
- **Authorization implied**: this delivery file authorizes nothing beyond confirming the document corpus is internally consistent at SHA `81ff802`. Ship requires Rule 8 PASS, which only W0+ implementation produces.

## Recorded by

- Author: Chao Xing (chaos.xing.xc@gmail.com)
- Cycle: 2026-05-08 remediation cycle 6 (architectural commit `81ff802`; this delivery file added in the cycle-6 audit-trail commit)
- Architecture-sync log: `gate/log/81ff802-posix.json`
- Manifest: `docs/governance/evidence-manifest.yaml`
- Predecessor delivery: `docs/delivery/2026-05-08-302337f.md` (cycle-5)
- Next gate: W0 produces the runnable artifact; the operator-shape smoke gate switches to real-flow PASS at that point.
