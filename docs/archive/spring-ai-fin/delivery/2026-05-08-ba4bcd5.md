# Delivery evidence -- 2026-05-08 -- SHA ba4bcd5

> Cycle-7 architecture-sync evidence at architectural SHA `ba4bcd5`.
> Per `docs/delivery/README.md`, this file documents an architecture-sync gate
> pass; it is **NOT** Rule 8 operator-shape evidence (no real LLM run, no
> long-lived process, no sequential N>=3 dependency runs).
>
> Two-SHA model (cycle-7 sec-B1):
> - `reviewed_content_sha = ba4bcd5` (architectural commit)
> - `evidence_commit_sha = HEAD` (audit-trail commit; one parent = ba4bcd5)
> - `evidence_commit_classification = audit_trail_only`
> - audit-trail diff is a strict subset of `allowed_audit_trail_paths`.

## SHA-current confirmation

- `git status` reports a clean working tree at the audit-trail commit's parent (`ba4bcd5`).
- The audit-trail commit's diff vs `ba4bcd5` is restricted to:
  - `docs/delivery/2026-05-08-ba4bcd5.md` (this file)
  - `docs/governance/evidence-manifest.yaml`
  - `docs/governance/architecture-status.yaml`
  - `docs/governance/current-architecture-index.md`
  - `gate/log/ba4bcd5-posix.json`

## Architecture-sync gate run (POSIX bash, ba4bcd5)

- Script: `gate/check_architecture_sync.sh`
- Version banner: `cycle-7-expanded`
- Platform: `posix`
- Result: `semantic_pass=true`, `evidence_valid_for_delivery=true` (PASS at audit-trail HEAD)
- Log naming: cycle-7 sec-B1 bootstrap policy declines to name a log file
  keyed to `reviewed_content_sha` because no gate run can have produced one
  (the manifest pointing to ba4bcd5 only exists in the audit-trail commit's
  tree). The PASS observed at audit-trail HEAD is the canonical evidence.

The gate enforces (cycle-7 sec-B1, B2, B3, D1, D2, D3, E1):

- `audit_trail_shape` -- HEAD is either reviewed_content_sha or a one-parent
  audit-trail commit whose diff is a subset of `allowed_audit_trail_paths`.
- `manifest_freshness` -- manifest's `reviewed_content_sha` is reachable from
  HEAD (via `git merge-base --is-ancestor`); `delivery_file` exists.
- `manifest_edge_consistency` -- status YAML references `reviewed_content_sha`;
  index references `delivery_file` basename.
- `delivery_log_parity` (extended) -- delivery file references SHA + names a
  log file under `gate/log/<sha>-*.json` + `semantic_pass=true` +
  `evidence_valid_for_delivery=true`.
- `ascii_only_governance` -- 8 governance files are ASCII-only.
- `capability_legacy_bucket` -- new findings using `capability:
  operator_shape_gate` without `legacy_capability:` marker are rejected.
- `gate_runtime_error` -- the gate body is wrapped in error-trapping; an
  unexpected runtime error is reported as a structured failure rather than
  exiting silently.

## Architecture-sync gate run (Windows PowerShell)

Not run in the cycle-7 author's environment. The PowerShell script is the
parity implementation of the POSIX one. Refer to `gate/log/<sha>-windows.json`
when produced from a Windows host.

PowerShell crash class fixed in cycle-7 sec-A1: variable initialization moved
to the shared block before all rule bodies. Validated by the new self-test
harness `gate/test_architecture_sync_gate.sh` (cycle-7 sec-E1) which performs
schema-aware structured-output assertions.

## Operator-shape gate

Not run for this delivery. Per `docs/governance/architecture-status.yaml`:

- `architecture_sync_gate.status = implemented_unverified` (architecture-sync only).
- `operator_shape_smoke_gate.status = design_accepted` (Rule 8 unblocks at W0).

## Cycle-7 deliverables consolidated at this SHA

This delivery covers the cycle-7 review's nine substantive findings:

- A1 -- PowerShell crash fix (variable initialization order); REM-2026-05-08-C7-1.
- A2 -- PowerShell parity with POSIX `delivery_log_parity` (extended);
  REM-2026-05-08-C7-2.
- B1 -- Two-SHA evidence model: `reviewed_content_sha` vs `evidence_commit_sha`;
  manifest schema v2; `audit_trail_shape` rule; REM-2026-05-08-C7-3.
- B2 -- `manifest_edge_consistency` rule (status + index ledger parity);
  REM-2026-05-08-C7-4.
- B3 -- Schema metadata (`schema_capabilities` booleans + `schema_version`);
  REM-2026-05-08-C7-5.
- D1 -- Maturity-as-primary status note; status field renamed to
  `evidence_state` is W1 deliverable; REM-2026-05-08-C7-6.
- D2 -- `legacy_capability` migration: 19 historical findings re-keyed;
  `capability_legacy_bucket` gate rule; REM-2026-05-08-C7-7.
- D3 -- ASCII-only active governance; `ascii_only_governance` gate rule covers
  8 files; REM-2026-05-08-C7-8.
- E1 -- Gate self-test harness `gate/test_architecture_sync_gate.sh`;
  REM-2026-05-08-C7-9.

## Cycle-7 acknowledged-only findings (not blocking)

- C1 -- Rule 8 state vs maturity parity (status `fail_closed_artifact_missing`
  + maturity L0 explicit pairing) is a W1 deliverable. Not blocking ship of
  the cycle-7 architectural set.
- C2 -- Test reference state taxonomy (every test reference declares
  `planned`/`stub`/`green` state). Manifest now exposes
  `test_reference_state_default: planned` as the document-wide default.
  Per-row enrichment is W1.

## Known constraints (carried forward)

- Rule 8 operator-shape smoke remains `fail_closed_artifact_missing` until W0
  produces `pom.xml` + Maven trees + a long-lived runnable process. The W0 plan
  in `docs/plans/W0-evidence-skeleton.md` defines the unblocking conditions.
- The `windows` log slot for cycle-7 is null because the author's environment
  lacks PowerShell. PowerShell parity is asserted by the parity script + the
  self-test harness; a Windows run will produce
  `gate/log/ba4bcd5-windows.json` when available.

## Audit-trail commit shape

- HEAD parent: `ba4bcd5` (architectural commit -- "Architecture remediation
  cycle 7: gate runtime integrity, two-SHA model, ASCII governance").
- HEAD diff vs parent: governance + delivery + log only (subset of
  `allowed_audit_trail_paths`).
- Audit-trail does not modify any L0/L1/L2 ARCHITECTURE document, any
  cross-cutting policy file, any plan, or any source code -- the architectural
  commit at `ba4bcd5` is fully self-contained. The audit-trail merely declares
  the evidence pointers.
