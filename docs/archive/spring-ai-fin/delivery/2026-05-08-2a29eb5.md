# Delivery evidence -- 2026-05-08 -- SHA 2a29eb5

> Cycle-11 OSS BoM + verification ladder evidence at architectural SHA `2a29eb5`.
> Per cycle-9 sec-A2: `evidence_valid_for_delivery=true`. Per cycle-9
> sec-D2: `dependency_mode: not_applicable`, `rule_8_eligible: false`.
> Per cycle-9 sec-E1: maturity remains L0 across all refresh
> capabilities; OSS deps split at U0 (most) + U1 (Spring AI / Temporal /
> MCP after upstream verification). Score is a secondary diagnostic.

## SHA-current confirmation

Audit-trail commit's diff vs `2a29eb5` is restricted to:

- `docs/delivery/2026-05-08-2a29eb5.md` (this file)
- `docs/governance/evidence-manifest.yaml`
- `docs/governance/architecture-status.yaml`
- `docs/governance/current-architecture-index.md`
- `gate/log/2a29eb5-posix.json`
- `gate/log/self-test-2a29eb5.json`

## Architecture-sync gate run (POSIX bash, 2a29eb5)

| Field | Value |
|---|---|
| Script | `gate/check_architecture_sync.sh` |
| Version banner | `cycle-9-truth-cut` |
| Platform | `posix` |
| `semantic_pass` | `true` |
| `evidence_valid_for_delivery` | `true` |

Windows: `missing_blocker` (no pwsh).

Self-test: `pass` (mode=local-smoke; pass_type=delivery_valid_pass).

## What this delivery covers

Cycle-11 closes the OSS-integration-correctness gap raised by user
question on 2026-05-09:

1. New active doc `docs/cross-cutting/oss-bill-of-materials.md` with:
   - U0..U4 OSS verification ladder (parallel to L0..L4 capability maturity).
   - Per-dep entry: groupId / artifactId / exact version / status /
     verification level / cited APIs / glue / fallback / risks.
   - Per-wave verification advancement plan.

2. L0 sec-2.1.5 added: OSS verification ladder summary + reference to
   the BoM doc.

3. Stale version corrections after upstream verification:
   - Spring AI: 1.0.x -> **1.0.7** (GA 2025-05-20; latest 1.0.x patch
     released 2026-05-08).
   - Temporal Java SDK: 1.25.x -> **1.34.0** (GA; `Workflow.getVersion`
     API confirmed at the role we cite).
   - MCP Java SDK: latest 0.x -> **2.0.0-M2** (`io.modelcontextprotocol.sdk:mcp`;
     **MILESTONE, not GA** -- API may change at 2.0.0 GA; risk noted).

4. L2 docs corrected: `agent-runtime/llm`, `temporal`, `tool` reflect
   the new pinned versions and U1 verification level.

5. Status YAML: REM-2026-05-09-C11-1..3 closure rows + REM-C11-CF
   carry-forward (pgvector vs Qdrant trigger criteria; Temporal
   alternative; MCP 2.0.0 GA contingency; Spring AI 1.0 vs 1.1 branch
   decision).

## Operator-shape gate (cycle-9 sec-D2)

Not run for this delivery. `manifest.rule_8.state =
fail_closed_artifact_missing`. `dependency_mode: fake;
rule_8_eligible: false`. This delivery does NOT claim Rule 8 PASS.

## Honest gaps remaining

- No dep is at U2 today. No Maven artifacts pulled; no probe code.
  The architecture's API claims are plausible but unverified by
  compile.
- MCP Java SDK at 2.0.0-M2 milestone -- API may change at 2.0.0 GA.
  W3 wave plan must include a 2.0.0-GA upgrade contingency.
- Architecture remains design-only per user's 2026-05-09 scope rule.

## Audit-trail commit shape

- HEAD parent: `2a29eb5` (architectural commit -- "Architecture
  remediation cycle 11: OSS bill of materials + verification ladder").
- HEAD diff vs parent: governance + delivery + log only (subset of
  `allowed_audit_trail_paths`).
