# Delivery evidence -- 2026-05-08 -- SHA 71b77c6

> Cycle-12 Phase A close evidence at architectural SHA `71b77c6`.
> Per cycle-9 sec-A2 / D2 / E1: `evidence_valid_for_delivery=true`;
> `dependency_mode: not_applicable`; `rule_8_eligible: false`; every
> refresh capability remains maturity **L0**.
>
> **Phase A is complete at this SHA.** Phase B trigger: pull OSS deps
> to local Maven cache (per user 2026-05-09 instruction).

## SHA-current confirmation

Audit-trail commit's diff vs `71b77c6` is restricted to:

- `docs/delivery/2026-05-08-71b77c6.md` (this file)
- `docs/governance/evidence-manifest.yaml`
- `docs/governance/architecture-status.yaml`
- `docs/governance/current-architecture-index.md`
- `gate/log/71b77c6-posix.json`
- `gate/log/self-test-71b77c6.json`

## Architecture-sync gate run

| Field | Value |
|---|---|
| Script | `gate/check_architecture_sync.sh` |
| Version banner | `cycle-9-truth-cut` |
| Platform | `posix` |
| `semantic_pass` | `true` |
| `evidence_valid_for_delivery` | `true` |

Windows: `missing_blocker` (no pwsh).

Self-test: `pass` (mode=local-smoke; pass_type=delivery_valid_pass).

## What this delivery covers

Cycle-12 closes the three remaining Phase A items I identified after
the user adopted the 2-phase Conceptual / Detail model:

1. **Interaction sequences** -- `docs/cross-cutting/interaction-sequences.md`
   with 6 flows: happy-path sync run; LLM provider outage; Postgres tx
   fail mid-run; tenant suspended mid-run; Temporal long-run; idempotent
   retry.

2. **V1 customer profile** -- `docs/cross-cutting/v1-customer-profile.md`
   pinning Tier-1 financial-services operator on-prem with concrete
   numbers (5 tenants / 500 users / 20k runs/day / 50 RPS sustained /
   7-year audit retention). Anchors NFR doc's prod targets.

3. **Design decision register** -- `docs/cross-cutting/design-decisions.md`
   with 15 ADRs (Java 21 + Spring Boot 3.5; Spring AI 1.0.7; Temporal
   1.34.0; Postgres + pgvector; SET LOCAL RLS GUC; ActionGuard 5-stage;
   Postgres outbox; OPA sidecar; Vault OSS; Keycloak; Spring Cloud
   Gateway; Maven; UUIDv7; 3-posture model; multi-framework dispatch
   deferral) plus 5 deferred decisions.

L0 enhancements:

- New sec-11.5 Phase A status -- marks Phase A complete; names the
  Phase B trigger (pull OSS deps).
- Sec-11 companion-docs table extended.

Governance: active-corpus + index + status YAML updated.
REM-2026-05-09-C12-1..4 (closure rows + Phase A complete marker).

## Phase A -> Phase B transition

**Phase A is complete.** All conceptual artifacts in tree:

| Artifact | Path | Status |
|---|---|---|
| L0 architecture | `ARCHITECTURE.md` | complete |
| L1 -- agent-platform | `agent-platform/ARCHITECTURE.md` | complete |
| L1 -- agent-runtime | `agent-runtime/ARCHITECTURE.md` | complete |
| 16 active L2 modules | `agent-{platform,runtime}/<module>/ARCHITECTURE.md` | complete |
| agent-eval | `agent-eval/ARCHITECTURE.md` | complete (W4 module) |
| 13 cross-cutting policies | `docs/cross-cutting/*.md` | complete |
| Engineering plan | `docs/plans/engineering-plan-W0-W4.md` | complete |
| Systems engineering plan | `docs/plans/architecture-systems-engineering-plan.md` | complete |
| 240+ dim self-audit | `docs/architecture-design-self-audit.md` | Round 4 complete |
| Systematic review | `docs/architecture-design-systematic-review-2026-05-09.md` | cycle-10 complete |
| OSS BoM | `docs/cross-cutting/oss-bill-of-materials.md` | cycle-11 complete (U1 critical-path) |

**Phase B trigger** (next step, per user instruction "after Phase A is
complete, the core work is to download the dependent open-source
components locally"):

1. Add root `pom.xml` (Maven multi-module: `agent-platform`,
   `agent-runtime`, `agent-eval`).
2. Pin OSS versions per `docs/cross-cutting/oss-bill-of-materials.md`.
3. Run `mvn -B dependency:resolve` to populate the local Maven cache.
4. Advance critical-path deps from U1 to U2 by adding probe code that
   imports the cited APIs from each dep (Spring AI ChatClient, Temporal
   `Workflow.getVersion`, MCP tool registration, etc.).
5. Begin module-by-module LLD starting with W0 (`agent-platform/web`
   health endpoint + minimal Spring Boot main + Postgres health
   query).

After step 1-3 land, the U2 column in the BoM doc starts filling in;
after step 4-5, capability maturity advances from L0 to L1 per
the engineering plan W0 acceptance gates.

## Score state (cycle-9 sec-E1 compliant)

Maturity headline: **every refresh capability remains L0** at cycle-12.
Phase A close moves no capability beyond L0 -- it only completes the
design surface. W0 is the next mover.

Secondary diagnostic (240+ dim self-audit): not re-scored at cycle-12;
the new G7 group already accommodated cycle-9 truth-cut, and cycle-10
plus cycle-11 plus cycle-12 additions slot into existing groups
(G4 cross-cutting policies for the 6 new docs; G6 coverage matrix
gains traceability via the design-decision register's reversal cost +
ADR rationale). Full re-score at W0 close where code dims unlock
level 5 promotions.

## Audit-trail commit shape

- HEAD parent: `71b77c6` (architectural commit -- cycle-12 close Phase A).
- HEAD diff vs parent: governance + delivery + log only (subset of
  `allowed_audit_trail_paths`).
