# Delivery evidence -- 2026-05-08 -- SHA e9a692d

> Cycle-9 truth-cut evidence at architectural SHA `e9a692d`.
> Per `docs/delivery/README.md`, this file documents an architecture-sync gate
> pass; it is **NOT** Rule 8 operator-shape evidence (no real LLM run, no
> long-lived process, no sequential N>=3 dependency runs).
>
> Two-SHA model (cycle-7 sec-B1; cycle-8 evidence graph v3):
> - `reviewed_content_sha = e9a692d` (architectural commit)
> - `evidence_commit_sha = HEAD` (audit-trail commit; one parent = e9a692d)
> - `evidence_commit_classification = audit_trail_only`
> - audit-trail diff is a strict subset of `allowed_audit_trail_paths`.
>
> Per cycle-9 sec-A2: this delivery file explicitly quotes
> `evidence_valid_for_delivery=true`. The committed gate log is the
> canonical evidence; stdout is a human convenience.
>
> Per cycle-9 sec-D2: this delivery file's evidence is from the
> architecture-sync gate only; `dependency_mode: not_applicable`,
> `rule_8_eligible: false`. This delivery does NOT and cannot claim
> Rule 8 PASS.
>
> Per cycle-9 sec-E1: this delivery file leads with **maturity (L0)**.
> Every refresh capability is L0; the design-only self-audit score is
> a secondary diagnostic and not a shipping claim.

## SHA-current confirmation

- The architectural commit `e9a692d` lands the cycle-9 truth-cut.
- The audit-trail commit's diff vs `e9a692d` is restricted to:
  - `docs/delivery/2026-05-08-e9a692d.md` (this file)
  - `docs/governance/evidence-manifest.yaml`
  - `docs/governance/architecture-status.yaml`
  - `docs/governance/current-architecture-index.md`
  - `gate/log/e9a692d-posix.json`
  - `gate/log/self-test-e9a692d.json`

## Architecture-sync gate run (POSIX bash, e9a692d)

| Field | Value |
|---|---|
| Script | `gate/check_architecture_sync.sh` |
| Version banner | `cycle-9-truth-cut` |
| Platform | `posix` |
| `semantic_pass` | `true` |
| `evidence_valid_for_delivery` | `true` |

Per the cycle-8 bootstrap-synthesis pattern (`gate/log/cc2e1e3-posix.json`
`_note` field), `gate/log/e9a692d-posix.json` was placed at this path
during audit-trail prep so the gate's `delivery_log_exact_binding` rule
finds it. The synthesis is transparent in the JSON `_note`. The gate
runs cleanly post-commit at the audit-trail HEAD (the present commit).

Note: `scan_files_count` drops from 67 (pre-cycle-9) to ~33 because
`active-corpus.yaml` was split per cycle-9 A1/B1 and only
`active_documents` are now scanned by `ascii_only_active_corpus`.
Transitional and historical documents are excluded by registry.

## Architecture-sync gate run (Windows PowerShell)

`pwsh` is not available in the cycle-9 author's environment. Per
cycle-8 sec-A3 + manifest schema v3, this is recorded as
`architecture_sync_logs.windows.state: missing_blocker`. Phase 2.4 of
the systemic plan (CI matrix with a Windows runner) is W1 and will
produce `gate/log/e9a692d-windows.json`.

## Self-test (cycle-8 sec-A2 delivery requirement)

| Field | Value |
|---|---|
| Self-test script | `gate/test_architecture_sync_gate.sh` |
| Mode | `local-smoke` |
| Result | `pass` |
| `pass_type` (cycle-9 sec-A2) | `delivery_valid_pass` |
| Pass SHA | `e9a692d` |
| Log | `gate/log/self-test-e9a692d.json` |

## Operator-shape gate (cycle-9 sec-D2 explicit)

Not run for this delivery, and the cycle-9 manifest fields make the
non-Rule-8 status explicit:

- `manifest.rule_8.state = fail_closed_artifact_missing`.
- `manifest.operator_shape_evidence.dependency_mode = fake`
  (CI smoke -- when added in W2 -- will be fake).
- `manifest.operator_shape_evidence.rule_8_eligible = false`.
- `manifest.operator_shape_evidence.provider_access_log_evidence = null`.

This delivery file does NOT claim Rule 8 PASS. The
`rule_8_state_consistency` gate rule (cycle-8 sec-C2) and the cycle-9
manifest fields together make any Rule 8 PASS claim mechanically
impossible while `dependency_mode=fake` or `artifact_present=false`.

## Cycle-9 deliverables consolidated at this SHA

This delivery covers cycle-9's 9 substantive findings:

- A1 / B1 -- 3-section corpus split (`active_documents` /
  `transitional_rationale` / `historical_documents`); gate rule
  `active_corpus_no_disposition_in_active`. REM-2026-05-09-C9-1, C9-3.
- A2 -- `local_semantic_pass` vs `delivery_valid_pass` distinction in
  manifest. REM-2026-05-09-C9-2.
- B2 -- index restructured; gate rule `index_active_subset`.
  REM-2026-05-09-C9-4.
- C1 -- 5-stage ActionGuard wins; 11-stage docs to
  `transitional_rationale`. REM-2026-05-09-C9-5.
- C2 -- gate rule `actionguard_5stage_invariants` replaces the legacy
  `actionguard_pre_post_evidence_missing`. REM-2026-05-09-C9-6.
- D1 -- ACCEPT-AS-CORRECT; W0 stays plan-only per user instruction;
  Rule 8 fail-closed unchanged. REM-2026-05-09-C9-7.
- D2 -- `dependency_mode` + `rule_8_eligible` + plan-level rule 8.
  REM-2026-05-09-C9-8.
- E1 -- maturity-first headline; percent score is secondary
  diagnostic. REM-2026-05-09-C9-9.

Plus G7 added to the self-audit rubric (5 dims; 22/20 + cross-link).

## Capability maturity (cycle-9 sec-E1 headline)

Every refresh capability is currently maturity **L0**. No code; no
tests; no Rule 8 PASS. Promotion to L1 requires a wave closing per
`docs/plans/engineering-plan-W0-W4.md`:

| Capability | Wave | Maturity now | Maturity at wave close |
|---|---|---|---|
| `agent-platform/web` | W0 | L0 | L1 |
| `agent-platform/auth` | W1 | L0 | L1 |
| `agent-platform/tenant` | W1 | L0 | L1 |
| `agent-platform/idempotency` | W1 | L0 | L1 |
| `agent-platform/bootstrap` | W0 | L0 | L1 |
| `agent-platform/config` | W2 | L0 | L1 |
| `agent-platform/contracts` | W0 | L0 | L1 |
| `agent-runtime/run` | W2 | L0 | L1 |
| `agent-runtime/llm` | W2 | L0 | L1 |
| `agent-runtime/tool` | W3 | L0 | L1 |
| `agent-runtime/action` | W3 | L0 | L1 |
| `agent-runtime/memory` | W2/W3 | L0 | L1 |
| `agent-runtime/outbox` | W2 | L0 | L1 |
| `agent-runtime/temporal` | W4 | L0 | L1 |
| `agent-runtime/observability` | W2 | L0 | L1 |
| `agent-eval` | W4 | L0 | L1 |

The 240+ dim self-audit (Round 4) records 982/985 design-time dims, but
this is **not a shipping claim**. It only states that the documentation
surface is internally consistent. Real readiness is the 32-dim scoring
framework (`docs/architecture-meta-reflection-2026-05-08.en.md`) which
remains 0/32 -- R/F categories unlock at W0+.

## Audit-trail commit shape

- HEAD parent: `e9a692d` (architectural commit -- "Architecture
  remediation cycle 9: truth-cut on active corpus + ActionGuard model").
- HEAD diff vs parent: governance + delivery + log only (subset of
  `allowed_audit_trail_paths`).
- Audit-trail does not modify any L0/L1/L2 ARCHITECTURE document, any
  cross-cutting policy file, any plan, any gate script, or any source
  code -- the architectural commit at `e9a692d` is fully self-contained.
  The audit-trail commit declares the evidence pointers + commits the
  gate log and self-test log.
