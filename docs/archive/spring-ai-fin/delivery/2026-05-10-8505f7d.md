# Delivery Evidence -- cycle-15/16 Defect Closure

**SHA**: `8505f7d`
**Date**: 2026-05-10
**Author**: Chao Xing
**Branch**: `fix/cycle-15-defect-closure`

---

## Root cause (Rule 1 four-line block)

**Observed failure**: Five core AutoConfigurations started cleanly in research/prod posture with L0 sentinel impls that throw 500 on first call; counter tags were inconsistent (`method`-only vs `spi+method`); SPI records lacked `tenantId` (Rule 11); README and ARCHITECTURE.md still claimed "Implementation has NOT started"; governance ledger had zero rows for 9 new starters; active-corpus doc files contained non-ASCII chars (em-dash, section-sign, right-arrow) that caused gate ascii_only_active_corpus failures.

**Execution path**: Cycles 15-16 delivered the W0 scaffold (14 Maven modules, SPI surface frozen) but did not complete the posture fail-fast wiring, counter tag standardization, SPI contract spine, doc refresh, ledger sync, or ASCII sanitization. The gate failures surfaced all of these as drift at the first gate run on this branch.

**Root cause**: W0 scaffold delivery (ec8daca) closed the build/test work but left engineering-posture, doc, ledger, and gate-compliance work incomplete, creating 4 categories of drift across 50+ files.

**Evidence**: `spring-ai-fin-memory-starter/.../MemoryAutoConfiguration.java` (posture check missing pre-fix); `ARCHITECTURE.md:218` (em-dash non-ASCII pre-fix); `docs/governance/architecture-status.yaml` (zero rows for 9 starters pre-fix); `README.md:97` ("Implementation has NOT started" pre-fix).

---

## Changes delivered (commits on fix/cycle-15-defect-closure)

| Commit | Defects | Description |
|---|---|---|
| `ce1b46a` | E2 | Add `tenantId` to SPI records (Rule 11 spine) |
| `97b0827` | E1+E3+E4 | Posture fail-fast + counter tag schema + slf4j dep |
| `0299011` | D1+D2+D3 | README + ARCHITECTURE.md + L1 docs refresh |
| `335e683` | G1+G2+G3 | Governance ledger: 11 new capability rows + evidence manifest |
| `52a7dbb` | -- | Interim delivery evidence (superseded) |
| `347f4e5` | gate | Fix em-dashes + section-sign in ARCHITECTURE.md, architecture-status.yaml |
| `8505f7d` | gate | Fix remaining em-dashes + right-arrows; reset evidence manifest placeholders |

---

## Verification

| Check | Result |
|---|---|
| `./mvnw verify -DskipITs=true` (14 modules) | BUILD SUCCESS |
| Unit tests (all starters) | 41+ pass, 0 fail |
| `ApiCompatibilityTest` (ArchUnit 4 rules) | GREEN |
| Posture fail-fast (research/prod reject sentinels) | GREEN |
| Counter tag schema `(spi, method)` | GREEN |
| `grep "pre-implementation" README.md` | 0 matches |
| `grep "spring-ai-fin-.*-starter" architecture-status.yaml` | >=9 matches |
| `check_spring_ai_milestone.sh` | PASS (2026-05-10 < 2026-08-01) |
| Non-ASCII chars in active corpus | 0 (fixed in 347f4e5 + 8505f7d) |

---

## Honest gaps (not in scope for this PR)

- Rule 8 real-dependency N>=3 runs: W4 deliverable.
- No real LLM call end-to-end (R7 still 0): W2 deliverable.
- Test LOC ~670 (R3 target >=1000): W1 adds default impl tests.
- G1 governance/product LOC ratio still ~4x: W1 product growth addresses.
- 5 sidecar starters skip posture fail-fast (enabled=false protects them).
- `HealthEndpointIT` requires Docker (Testcontainers); skipped locally, passes in CI.
