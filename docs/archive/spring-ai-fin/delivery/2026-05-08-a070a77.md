# Delivery 2026-05-08 a070a77

> **Classification: ARCHITECTURE-SYNC EVIDENCE.**
> NOT Rule 8 operator-shape evidence. The Rule 8 operator-shape gate (`gate/run_operator_shape_smoke.{ps1,sh}`) now exists and fails closed because no runnable artifact exists yet (W0 deliverable). Architecture-sync PASS does NOT authorize ship.
>
> Per cycle-3 SHA-current rule: this delivery file is evidence for SHA `a070a77` only.

## Cycle context

- **Cycle**: Architecture remediation cycle 4, addressing `docs/systematic-architecture-remediation-plan-2026-05-08-cycle-4.en.md` (8 findings: A1-A4, B1-B2, C1, D1, E1).
- **Predecessor delivery files**: `2026-05-08-d284232.md` (local-only), `2026-05-08-7025ac9.md` (cycle-1+2 first delivery-valid), `2026-05-08-003ed6f.md` (cycle-3 delivery-valid; predecessor-of-record).
- **Allowed claims** (per `docs/governance/closure-taxonomy.md`): `proposed`, `design_accepted`, `implemented_unverified`. No capability rises above `implemented_unverified`.

## Posture / shape

- `APP_POSTURE`: not applicable (no runnable artifact).
- `APP_DEPLOYMENT_SHAPE`: not applicable.

## Long-lived process

- start command: **none**. No buildable JAR.
- pid: **none**.
- uptime at gate end: **none**.

## Real dependencies

- Postgres URL: **none**.
- LLM provider: **none**.
- WORM target: **none**.

## Sequential runs

Not applicable.

## Cross-context resource stability

Not applicable.

## Lifecycle observability

Not applicable.

## Cancellation round-trip

Not applicable.

## What DID run for this SHA — Architecture-sync gate (cycle-4-expanded)

| Field | Value |
|---|---|
| Script | `gate/check_architecture_sync.sh` (POSIX) and `gate/check_architecture_sync.ps1` (PowerShell, Windows) |
| Version | `cycle-4-expanded` |
| Invocation | `bash gate/check_architecture_sync.sh` (no `--local-only`) |
| Outcome | **PASS** |
| `working_tree_clean` | `true` |
| `git_status_porcelain` | `""` (empty) |
| `semantic_pass` | `true` |
| `evidence_valid_for_delivery` | **`true`** |
| `local_only` | `false` |
| `scan_files_count` | `44` |
| Failures | `[]` |
| Log | `gate/log/a070a77.json` |

Cycle-4 gate enhancements verified at this SHA (rule identifiers; no raw forbidden phrases quoted here so this delivery file does not self-trigger):

1. **Locale fix (A1)**: bash gate exports `LC_ALL=C` so `grep -P` works in non-UTF-8 Windows locales (was silently skipping the closure-language regex set).
2. **Scope expansion (B2)**: rules `contract_posture_purity` and `auth_algorithm_policy` now scan ALL `agent-platform/**/ARCHITECTURE.md`, not just L1.
3. **Output path split (A3)**: delivery-valid logs go to `gate/log/<sha>.json`; everything else goes to `gate/log/local/<sha>.json` (gitignored). Failed local runs no longer pollute the working tree.
4. **New rule (C1)**: `rls_reset_vocabulary` flags stale HikariCP reset / `HikariConnectionResetPolicy` / connection check-in reset claims unless the line carries a negation/deprecation marker.
5. **Skip list (E1)**: scan now excludes `architecture-v5.0*`, `architecture-review-2026-05-07*`, `deep-architecture-security-assessment*` as historical (with banners).

## What DID run — Operator-shape smoke gate (fail-closed)

| Field | Value |
|---|---|
| Script | `gate/run_operator_shape_smoke.{ps1,sh}` (D1 deliverable) |
| Version | `cycle-4-fail-closed` |
| Outcome | **FAIL_ARTIFACT_MISSING** (exit 1) |
| `artifact_present` | `false` |
| `evidence_valid_for_delivery` | `false` |
| `rule_8_evidence` | `false` |
| Log | `gate/log/local/operator-shape-<sha>.json` (gitignored; pre-W0 fail-closed) |

Probed artifacts (none of which exist yet):

- `pom.xml`
- `agent-platform/pom.xml`
- `agent-runtime/pom.xml`
- `agent-platform/src/main/java`
- `agent-runtime/src/main/java`

The fail-closed status is the architecture's own honest assertion: there is no Rule 8 evidence at this SHA. W0 will produce the runnable artifact and the smoke gate will switch to a real-flow PASS.

## What is NOT yet runnable (W0 prerequisites)

- Maven multi-module build
- Source / test trees
- `JwksValidator`, `PostureBootGuard`, `ActionGuard`, `TenantBinder`, `SyncSagaOrchestrator`, etc.
- Real-flow operator-shape smoke gate

## Capability statuses on this SHA

Per `docs/governance/architecture-status.yaml`:

- All P0 + P1 + REM findings remain `design_accepted` and unblocked at this SHA.
- `operator_shape_gate` capability split:
  - architecture-sync portion: `implemented_unverified` (passes at HEAD with cycle-4 fixes; both PowerShell and POSIX scripts exist)
  - operator-shape smoke portion: `design_accepted` (fail-closed scripts exist as of cycle-4 D1; real-flow evidence requires W0)
- `REM-2026-05-08-C3-4` downgraded from `implemented_unverified` to `design_accepted` with `last_validated_sha: null`, until current HEAD passes (this SHA satisfies that condition).

## Cycle-4 Definition-of-Done check

| # | Item | Status |
|---|---|---|
| 1 | `git status --short` empty before delivery gate run | **Done** at `a070a77` |
| 2 | `gate/check_architecture_sync.ps1` exits 0 | **Done** (cycle-4-expanded; locale fix verified) |
| 3 | `gate/check_architecture_sync.sh` exits 0 | **Done** at `a070a77` |
| 4 | `gate/log/<sha>.json` has expected fields | **Done** (`gate/log/a070a77.json`) |
| 5 | `docs/delivery/2026-05-08-<sha>.md` exists for same SHA | **Done** (this file) |
| 6 | `agent-platform/**/ARCHITECTURE.md` no unqualified APP_JWT_SECRET | **Done** (bootstrap, cli, config qualified; gate scope now covers all platform L2s) |
| 7 | Current RLS docs no active "HikariCP reset" claim | **Done** (governance ledger + matrix + diagram updated; `rls_reset_vocabulary` rule enforces) |
| 8 | `gate/run_operator_shape_smoke.*` exists, fails closed | **Done** (both `.ps1` and `.sh`; exit 1 with structured JSON) |
| 9 | `architecture-status.yaml` does not promote beyond evidence level | **Done** (REM-C3-4 downgraded; `last_validated_sha` field added to gate capabilities) |

## Gate result

- **Architecture-sync gate**: **PASS** at SHA `a070a77`. `evidence_valid_for_delivery: true`.
- **Operator-shape smoke gate**: **FAIL_ARTIFACT_MISSING** (correct fail-closed behaviour pre-W0). Not Rule 8 evidence.
- **Authorization implied**: this delivery file authorizes nothing beyond confirming the document corpus is internally consistent at SHA `a070a77`. Ship authorization requires Rule 8 PASS, which only W0+ implementation can produce.

## Recorded by

- Author: Chao Xing (chaos.xing.xc@gmail.com)
- Cycle: 2026-05-08 remediation cycle 4 (commit `a070a77`; logs and delivery committed in subsequent commits)
- Architecture-sync log: `gate/log/a070a77.json`
- Operator-shape smoke log: `gate/log/local/operator-shape-a070a77.json` (gitignored; fail-closed pre-W0)
- Predecessor delivery: `docs/delivery/2026-05-08-003ed6f.md` (cycle-3)
- Next gate: W0 will produce the first runnable artifact; the operator-shape smoke gate switches to real-flow PASS at that point.
