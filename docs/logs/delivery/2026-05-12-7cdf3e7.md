# Delivery Record — 2026-05-12 — SHA 7cdf3e7

**Delivery SHA**: 7cdf3e79cfcc3560e328ca3fd3f2be384a2a8626
**Date**: 2026-05-12
**Directive**: Occam's Razor — three first principles (P1/P2/P3) + three engineering constraints (E1/E2/E3)
**Scope**: Full architecture simplification — 32 commits (C1–C27 + hidden inserts C9a, C9b, C16a, C16b, C18a)

---

## Before / After Metrics

| Dimension | Before | After | Delta |
|-----------|--------|-------|-------|
| Maven reactor modules | 14 | 6 (parent + 5) | -8 |
| SDK/SPI starters | 10 SPIs | 1 SPI + 1 concrete class | -9 |
| Markdown files | ~170 | ~108 | -62 |
| `architecture-status.yaml` lines | 1,952 | 259 | -1,693 |
| Gate rules | 27 | 6 | -21 |
| `CLAUDE.md` rules | 12 active + G1-G4 | 9 active + 2 deferred | -5 active |
| `gate/check_architecture_sync.sh` lines | 917 | 181 | -736 |
| `ARCHITECTURE.md` lines | 825 | 150 | -675 |
| `README.md` lines | 220 | 46 | -174 |

---

## Decision Rule Applied

| Artifact type | Decision |
|---|---|
| SPI wrapping an OSS Java interface (E1 violation) | DELETE — replaced by direct OSS usage |
| SPI with no Java OSS equivalent (graph memory, idempotency) | KEEP as E2 middleware shell or concrete class |
| Module whose only production code is a throwing sentinel | DELETE |
| `ARCHITECTURE.md` for a subdir with zero `.java` files | DEMOTE to `docs/v6-rationale/` or DELETE |
| Capability ledger row: `design_accepted`, `implementation: null`, no W0 commit | DELETE from YAML |
| Gate rule policing grammar in deleted/demoted docs | DELETE |
| Gate rule catching real defect class in shipped Java | KEEP |
| CLAUDE.md rule with no live target yet (operator-shape, contract spine) | DEFER to `CLAUDE-deferred.md` with trigger |

---

## Commit Boundary Summary

### Phase 1 — Code simplification

| Commit | Description |
|--------|-------------|
| C1 | Relocate `GraphMemoryRepository` SPI into `agent-runtime`; retarget ArchUnit rules |
| C2 | DELETE `spring-ai-ascend-resilience-starter` |
| C3 | DELETE `spring-ai-ascend-langchain4j-profile` + `spring-ai-ascend-docling-starter` |
| C4 | DELETE `spring-ai-ascend-mem0-starter` |
| C5 | DELETE `spring-ai-ascend-memory-starter` |
| C6 | DELETE `spring-ai-ascend-skills-starter` + `spring-ai-ascend-knowledge-starter` |
| C7 | DELETE `spring-ai-ascend-governance-starter`; add `IdempotencyStore` to `agent-platform` |
| C8 | DELETE `spring-ai-ascend-persistence-starter`; add `MemorySpiArchTest` ArchUnit |
| C9 | SHRINK `spring-ai-ascend-graphmemory-starter` to E2 middleware shell |
| C9a | Drop legacy `spring.factories` from graphmemory-starter (B.h1) |
| C9b | Add `spring-ai-ascend-graphmemory-starter/README.md` middleware-pattern doc (B.h2) |
| C10 | Consolidate `agent-runtime/` ARCHITECTURE.md tree; demote 10 to `docs/v6-rationale/` |
| C11 | BoM + status-ledger canonical-counts cleanup |

### Phase 2 — Doc + governance simplification

| Commit | Description |
|--------|-------------|
| C12 | Create `docs/v6-rationale/` holding pen |
| C13 | Demote 4 `agent-platform/` L2s to `docs/v6-rationale/` |
| C14 | Delete 3 dead + consolidate 3 `agent-platform/` L2s |
| C15 | Delete 5 dead + demote 5 cross-cutting docs |
| C16 | Demote 6 cross-cutting security docs to `docs/v6-rationale/` |
| C16a | Rename `docs/adr/0007-spi-first-extensibility.md` → `0007-outbox-postgres-not-kafka.md` (D.h1) |
| C16b | ADR forward-pointer footnotes for 15 ADRs referencing moved/deleted docs (D.h2) |
| C17 | Consolidate 6 docs into `docs/contracts/contract-catalog.md` |
| C18+C18a | Create `docs/observability/policy.md`; slim metric inventory to surviving namespaces (D.h3) |
| C19 | Fold integration + topology into README/runbooks; delete `AGENTS.md` |
| C20 | Slim `docs/cross-cutting/posture-model.md`; add PostureBootGuard W1 note (B.h3) |
| C21 | Slim `docs/cross-cutting/non-functional-requirements.md` |
| C22 | Slim `architecture-status.yaml` 1,952 → 259 lines |
| C23 | Delete 8 governance metadocs |
| C24 | Rewrite gate scripts 27 → 6 rules |
| C25 | Slim `CLAUDE.md`; create `docs/CLAUDE-deferred.md` (Rules 8 + 11 deferred) |
| C26 | Slim root `ARCHITECTURE.md` 825 → 150 lines |
| C27 | Slim `README.md`; create `docs/STATE.md` with D.h4 schema |

---

## Hidden Findings Surfaced by Categorical Self-Audit

| Finding | Category | Status |
|---------|----------|--------|
| B.h1 — dual `spring.factories` + `AutoConfiguration.imports` in graphmemory-starter | B — E2 shell integrity | Fixed (C9a) |
| B.h2 — no README on middleware-shell starter; shell vs broken indistinguishable | B — E2 shell integrity | Fixed (C9b) |
| B.h3 — PostureBootGuard described in posture-model.md with no Java | B — E2 shell integrity | Fixed (C20: one-line W1 note) |
| D.h1 — `docs/adr/0007-spi-first-extensibility.md` filename/title mismatch | D — Doc truthfulness | Fixed (C16a) |
| D.h2 — 15 ADRs reference paths C10/C15-C19 moved or deleted | D — Doc truthfulness | Fixed (C16b) |
| D.h3 — `observability/policy.md` inherited metric names from 7 deleted starters | D — Doc truthfulness | Fixed (C18a) |
| D.h4 — `docs/STATE.md` schema not specified | D — Doc truthfulness | Fixed (C27: 6-column schema) |
| F.h1 — Rule 7 aspirational after L0 sentinels deleted; no live fallback paths | F — Rule self-consistency | Fixed (C25: guard sentence added) |
| F.h2 — `tdd-red-sha` clause in Rule 4 never enforced; zero occurrences in codebase | F — Rule self-consistency | Fixed (C25: clause dropped) |
| F.h3 — Rule 10 posture matrix listed 14 starters; 9 deleted | F — Rule self-consistency | Fixed (C25: matrix shrunk) |

---

## Surviving Architecture

**5 reactor modules** (down from 14):

```
spring-ai-ascend-dependencies (BoM)
    ├── agent-platform          (HTTP, tenant filter, idempotency, health)
    ├── agent-runtime           (OssApiProbe + GraphMemoryRepository SPI)
    ├── spring-ai-ascend-graphmemory-starter  (E2 middleware shell)
    └── agent-eval              (W4 placeholder)
```

**1 surviving SPI**: `GraphMemoryRepository` in `agent-runtime/src/main/java/com/huawei/ascend/runtime/memory/spi/`

**1 surviving cross-cutting concrete class**: `IdempotencyStore` in `agent-platform/src/main/java/com/huawei/ascend/platform/idempotency/`

**OSS replacements for deleted SPIs:**
- `LongTermMemoryRepository` → Spring AI `VectorStore` + `ChatMemory`
- `ToolProvider` → Spring AI `ToolCallbackProvider` + `@Tool`
- `LayoutParser` / `DocumentSourceConnector` → Spring AI `DocumentReader` family
- `PolicyEvaluator` → Spring Security `AuthorizationManager`
- `ResilienceContract` → Resilience4j `@CircuitBreaker` + YAML
- `RunRepository` / `ArtifactRepository` → Spring Data JDBC `CrudRepository` (W1)

---

## Gate Status

**Architecture-sync gate** (`gate/check_architecture_sync.sh`): PASS
```
PASS: status_enum_invalid
PASS: delivery_log_parity
PASS: eol_policy
PASS: ci_no_or_true_mask
PASS: required_files_present
PASS: metric_naming_namespace
GATE: PASS
```

**Gate self-test** (`gate/test_architecture_sync_gate.sh`): Tests passed: 12/12

**Full reactor build** (`mvn clean test`): BUILD SUCCESS

**Operator-shape gate (Rule 8)**: Deferred. Rule 8 is in `docs/CLAUDE-deferred.md`; re-introduction trigger is first shippable jar with a real external dependency booting under a process supervisor.

---

## Open / Deferred Items

| Item | Deferred to | Trigger |
|------|------------|---------|
| Rule 8 — Operator-Shape Readiness Gate | W2 | First shippable jar with real LLM/DB under process supervisor |
| Rule 11 — Contract Spine Completeness | W2 | First persistent record class (`RunRecord`, `IdempotencyRecord`, etc.) committed |
| `IdempotencyStore` against real Postgres | W1 | `idempotency_store_postgres` capability |
| `RunRepository` / `ArtifactRepository` CrudRepository impl | W2 | `run_lifecycle` capability |
| `PostureBootGuard` / `RequiredConfig` | W1 | `posture_module_bootstrap` capability |
| Liveness/readiness probe split | W2 | `health_endpoint_liveness_split` capability |
