# DFX (Design-for-X) declaration for the agent-client module.
#
# Required by CLAUDE.md Rule 32 — every module declared `kind: domain` MUST
# publish a DFX yaml covering all five dimensions.
#
# Status: skeleton. The module is a deliberately empty placeholder so the
# AgentClient team has a workspace. Bodies are pending until SDK code lands
# (W3+ per ADR-0049).

module: agent-client
kind: domain
version: 1
generated_at: 2026-05-17

releasability:
  artifact_coords: "io.github.springai-ascend:agent-client"
  semver_policy: "experimental — module is a skeleton; no public API surface yet"
  deprecation_window: "N/A — pre-1.0 SDK"
  packaging: "library jar (planned: published SDK consumed by downstream apps)"
  status: pending

resilience:
  fallback_paths: []
  posture_aware_defaults: "pending — SDK will expose retry/backoff policy at W3"
  cancellation_contract: "pending — Task Cursor cancellation per Rule 36 / P-F"
  reference_rule: "Rule 36 (Cursor Flow Mandate) — runtime contract the SDK consumes"
  status: pending

availability:
  health_endpoints: []
  readiness_signal: "N/A — client-side SDK; readiness is owned by the consuming application"
  startup_failure_modes: []
  status: pending

vulnerability:
  scan_tooling: "pending — Snyk / OWASP dependency check planned at first release"
  tenant_isolation: "client carries tenant identity via JWT; the SDK does not persist tenant state"
  payload_integrity: "client respects §4 #13 16-KiB inline-payload cap; larger payloads use blob refs"
  spi_purity: "N/A — module declares no SPI"
  status: pending

observability:
  metric_namespace: "springai_ascend_client_* (planned)"
  trace_carrier: "pending — SDK will propagate W3C traceparent on submission"
  hook_emission_path: "N/A — SDK is a consumer, not a hook producer"
  audit_trail: "N/A — audit lives server-side"
  status: pending
