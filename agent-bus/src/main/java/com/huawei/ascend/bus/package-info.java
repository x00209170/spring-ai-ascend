/**
 * agent-bus — Bus & State Hub plane (skeleton).
 *
 * <p>Empty placeholder package. Runtime implementations of the W2 bus SPIs
 * (workflow intermediary, mailbox, admission control, backpressure signalling,
 * sleep/wakeup declaration, and the tick engine — enumerated by name in
 * ADR-0050) land in W2. The contract surface for the three-track channel
 * isolation already ships in {@code docs/governance/bus-channels.yaml}
 * (Rule 35 / P-E).
 *
 * <p>Authority: ADR-0050, Layer-0 principles P-E + P-I, Rule 35.
 */
package com.huawei.ascend.bus;
